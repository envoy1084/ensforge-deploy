---
title: useWorkflows
description: List workflows for the configured chain and wallet account.
---

import ParametersGuide from "../../../../../shared/react/atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/atom-result.mdx";

# useWorkflows

List workflows for the configured chain and wallet account. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useWorkflows } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useWorkflows } from "@ensforge/react";

function Component() {
  const result = useWorkflows({
    status: "pending",
    limit: 20,
  });

  return (
    <pre>
      {JSON.stringify(
        result.data,
        (_, value) => (typeof value === "bigint" ? value.toString() : value),
        2,
      )}
    </pre>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

## Parameters

### walletClient

`WalletClient | undefined`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

### after

`string | undefined`

Exclusive pagination cursor from the previous response.

### limit

`number | undefined`

Page size between 1 and 100.

### status

`"pending" | "completed" | undefined`

Optional pending/completed workflow filter.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useWorkflows>;
```

<ResultGuide />

## Effect Atom

```ts
import { listWorkflowsAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = listWorkflowsAtom(sdk, parameters, options);
```

## Action

- [`listWorkflows`](/core/api/actions/workflows/list-workflows)
- [`sdk.workflows.listWorkflows`](/sdk/api/workflows/list-workflows)
