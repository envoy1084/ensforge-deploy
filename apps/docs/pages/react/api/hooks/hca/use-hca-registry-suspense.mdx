---
title: useHcaRegistrySuspense
description: Inspect module registry state
---

import ParametersGuide from "../../../../../shared/react/suspense-atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/suspense-atom-result.mdx";
import { ReadActionDemo } from "../../../../../components/read-actions/read-action-demo.client.tsx";

# useHcaRegistrySuspense

Inspect module registry state. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useHcaRegistrySuspense } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useHcaRegistrySuspense } from "@ensforge/react";

function Component() {
  const result = useHcaRegistrySuspense({
    hca: "0x1234567890123456789012345678901234567890",
  });

  return (
    <pre>
      {JSON.stringify(
        result.data,
        (_, value) => (typeof value === "bigint" ? value.toString() : value),
        2,
      )}
    </pre>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

<ReadActionDemo action="hca.getHcaRegistry" />

## Parameters

```ts
import type { UseEnsSuspenseAtomParameters } from "@ensforge/react";
import type { HcaReadParameters } from "@ensforge/sdk/hca";
```

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useHcaRegistrySuspense>;
```

<ResultGuide />

## Effect Atom

```ts
import { getHcaRegistryAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = getHcaRegistryAtom(sdk, parameters, options);
```

## Action

- [`getHcaRegistry`](/core/api/actions/hca/get-hca-registry)
- [`sdk.hca.getHcaRegistry`](/sdk/api/hca/get-hca-registry)
