---
title: useHcaUpgradeEligibilitySuspense
description: Check the candidate implementation and both upgrade gates
---

import ParametersGuide from "../../../../../shared/react/suspense-atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/suspense-atom-result.mdx";

# useHcaUpgradeEligibilitySuspense

Check the candidate implementation and both upgrade gates Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useHcaUpgradeEligibilitySuspense } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useHcaUpgradeEligibilitySuspense } from "@ensforge/react";

function Component(parameters: Parameters<typeof useHcaUpgradeEligibilitySuspense>[0]) {
  const result = useHcaUpgradeEligibilitySuspense(parameters);
  return <p>{result.isWaiting ? "Loading" : "Result available"}</p>;
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

## Parameters

```ts
type Input = Parameters<typeof useHcaUpgradeEligibilitySuspense>[0];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useHcaUpgradeEligibilitySuspense>;
```

<ResultGuide />

## Effect Atom

```ts
import { getHcaUpgradeEligibilityAtom } from "@ensforge/react/atoms";

const atom = getHcaUpgradeEligibilityAtom(sdk, parameters, options);
```

## Action

- [`getHcaUpgradeEligibility`](/core/api/actions/hca/get-hca-upgrade-eligibility)
- [`sdk.hca.getHcaUpgradeEligibility`](/sdk/api/hca/get-hca-upgrade-eligibility)
