---
title: useIsHcaModuleInstalled
description: Check a module type/address
---

import ParametersGuide from "../../../../../shared/react/atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/atom-result.mdx";

# useIsHcaModuleInstalled

Check a module type/address Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useIsHcaModuleInstalled } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useIsHcaModuleInstalled } from "@ensforge/react";

function Component(parameters: Parameters<typeof useIsHcaModuleInstalled>[0]) {
  const result = useIsHcaModuleInstalled(parameters);
  return <p>{result.isWaiting ? "Loading" : "Result available"}</p>;
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

## Parameters

```ts
type Input = Parameters<typeof useIsHcaModuleInstalled>[0];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### moduleTypeId

`bigint`

ERC-7579 module type identifier.

### module

`` `0x${string}` ``

Module contract address.

### additionalContext

`` `0x${string}` | undefined ``

Module-specific inspection context.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useIsHcaModuleInstalled>;
```

<ResultGuide />

## Suspense

Use [`useIsHcaModuleInstalledSuspense`](/react/api/hooks/hca/use-is-hca-module-installed-suspense) beneath Suspense and an error boundary.

## Effect Atom

```ts
import { isHcaModuleInstalledAtom } from "@ensforge/react/atoms";

const atom = isHcaModuleInstalledAtom(sdk, parameters, options);
```

## Action

- [`isHcaModuleInstalled`](/core/api/actions/hca/is-hca-module-installed)
- [`sdk.hca.isHcaModuleInstalled`](/sdk/api/hca/is-hca-module-installed)
