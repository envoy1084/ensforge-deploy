---
title: useWithdrawHcaDeposit
description: Use the validated owner self-call withdrawal route
---

# useWithdrawHcaDeposit

Use the validated owner self-call withdrawal route Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useWithdrawHcaDeposit } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useWithdrawHcaDeposit } from "@ensforge/react";

function Component({
  parameters,
}: {
  parameters: Parameters<ReturnType<typeof useWithdrawHcaDeposit>["mutate"]>[0];
}) {
  const mutation = useWithdrawHcaDeposit();
  return (
    <button disabled={mutation.isWaiting} onClick={() => mutation.mutate(parameters)}>
      Run
    </button>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

## Parameters

Pass operation parameters to `mutate`, `mutateAsync` or `mutateEffect`. Hook options accept `onExit` and an explicit retry policy.

```ts
type Input = Parameters<ReturnType<typeof useWithdrawHcaDeposit>["mutate"]>[0];
```

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

### confirmation

`{ readonly type: "submitted"; } | { readonly type: "confirmed"; readonly timeout?: number | undefined; readonly confirmations?: number | undefined; }`

Receipt confirmation policy.

### walletClient

`Input["walletClient"]`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

### amount

`bigint`

Native token amount in wei.

### to

`` `0x${string}` ``

Destination address.

## Return Type

```ts
type Result = ReturnType<typeof useWithdrawHcaDeposit>;
```

The mutation exposes `data`, `error`, `isWaiting`, `result`, execution methods, `reset` and `interrupt`. Interruption stops local work; it does not undo a broadcast or revoke a session. See [Mutation Hooks](/react/guides/mutations).

## Effect Atom

```ts
import { createWithdrawHcaDepositMutationAtom } from "@ensforge/react/atoms";

const atom = createWithdrawHcaDepositMutationAtom(sdk);
```

## Action

- [`withdrawHcaDeposit`](/core/api/actions/hca/withdraw-hca-deposit)
- [`sdk.hca.withdrawHcaDeposit`](/sdk/api/hca/withdraw-hca-deposit)
