---
title: useHcaRegistrationSuspense
description: Inspect/reconcile saved registration
---

import ParametersGuide from "../../../../../shared/react/suspense-atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/suspense-atom-result.mdx";

# useHcaRegistrationSuspense

Inspect/reconcile saved registration Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useHcaRegistrationSuspense } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useHcaRegistrationSuspense } from "@ensforge/react";

function Component(parameters: Parameters<typeof useHcaRegistrationSuspense>[0]) {
  const result = useHcaRegistrationSuspense(parameters);
  return <p>{result.isWaiting ? "Loading" : "Result available"}</p>;
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

## Parameters

```ts
type Input = Parameters<typeof useHcaRegistrationSuspense>[0];
```

### id

`string`

Saved operation identifier. Start registration can generate it from matching unfinished work.

### storage

`WorkflowStorage | HcaRegistrationStorage | undefined`

Optional storage override. Defaults to config storage.

### execution

`HcaRegistrationExecution | undefined`

Compatible execution adapter. Omit for direct owner execution when supported.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useHcaRegistrationSuspense>;
```

<ResultGuide />

## Effect Atom

```ts
import { getHcaRegistrationAtom } from "@ensforge/react/atoms";

const atom = getHcaRegistrationAtom(sdk, parameters, options);
```

## Action

- [`getHcaRegistration`](/core/api/actions/hca/get-hca-registration)
- [`sdk.hca.getHcaRegistration`](/sdk/api/hca/get-hca-registration)
