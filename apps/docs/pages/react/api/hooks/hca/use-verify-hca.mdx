---
title: useVerifyHca
description: Verify account identity, profile, ownership and deployment wiring
---

import ParametersGuide from "../../../../../shared/react/atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/atom-result.mdx";

# useVerifyHca

Verify account identity, profile, ownership and deployment wiring. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useVerifyHca } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useVerifyHca } from "@ensforge/react";

function Component() {
  const result = useVerifyHca({
    hca: "0x1234567890123456789012345678901234567890",
  });

  return (
    <pre>
      {JSON.stringify(
        result.data,
        (_, value) => (typeof value === "bigint" ? value.toString() : value),
        2,
      )}
    </pre>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

## Parameters

```ts
import type { UseEnsAtomParameters } from "@ensforge/react";
import type { VerifyHcaParameters } from "@ensforge/sdk/hca";
```

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### expectedOwner

`` `0x${string}` | undefined ``

Expected account owner. A mismatch fails verification.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

### initialImplementation

`` `0x${string}` | undefined ``

Implementation used when deriving the account address, including an account that was later upgraded.

### allowUndeployed

`boolean | undefined`

Allow a counterfactual account after verifying its derivation.

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useVerifyHca>;
```

<ResultGuide />

## Suspense

Use [`useVerifyHcaSuspense`](/react/api/hooks/hca/use-verify-hca-suspense) beneath Suspense and an error boundary.

## Effect Atom

```ts
import { verifyHcaAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = verifyHcaAtom(sdk, parameters, options);
```

## Action

- [`verifyHca`](/core/api/actions/hca/verify-hca)
- [`sdk.hca.verifyHca`](/sdk/api/hca/verify-hca)
