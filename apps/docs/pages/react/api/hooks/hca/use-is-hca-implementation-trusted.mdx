---
title: useIsHcaImplementationTrusted
description: Check trusted implementation membership
---

import ParametersGuide from "../../../../../shared/react/atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/atom-result.mdx";

# useIsHcaImplementationTrusted

Check trusted implementation membership. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useIsHcaImplementationTrusted } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useIsHcaImplementationTrusted } from "@ensforge/react";

function Component() {
  const result = useIsHcaImplementationTrusted({
    implementation: "0x5678901234567890123456789012345678901234",
  });

  return (
    <pre>
      {JSON.stringify(
        result.data,
        (_, value) => (typeof value === "bigint" ? value.toString() : value),
        2,
      )}
    </pre>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

## Parameters

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useIsHcaImplementationTrusted>;
```

<ResultGuide />

## Suspense

Use [`useIsHcaImplementationTrustedSuspense`](/react/api/hooks/hca/use-is-hca-implementation-trusted-suspense) beneath Suspense and an error boundary.

## Effect Atom

```ts
import { isHcaImplementationTrustedAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = isHcaImplementationTrustedAtom(sdk, parameters, options);
```

## Action

- [`isHcaImplementationTrusted`](/core/api/actions/hca/is-hca-implementation-trusted)
- [`sdk.hca.isHcaImplementationTrusted`](/sdk/api/hca/is-hca-implementation-trusted)
