---
title: useDeployHca
description: Deploy or verify the existing deterministic account
---

import MutationOptions from "../../../../../shared/react/mutation-options.mdx";
import MutationResult from "../../../../../shared/react/mutation-result.mdx";

# useDeployHca

Deploy or verify the existing deterministic account. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useDeployHca } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useDeployHca } from "@ensforge/react";

function Component() {
  const mutation = useDeployHca();

  return (
    <button
      disabled={mutation.isWaiting}
      onClick={() =>
        mutation.mutate({
          owner: "0x2345678901234567890123456789012345678901",
        })
      }
    >
      Deploy account
    </button>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

<MutationOptions />

## Mutation Parameters

```ts
import type { DeployHcaParameters } from "@ensforge/sdk/hca";
```

### owner

`` `0x${string}` ``

Owner address used for account derivation or authorization.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

### implementation

`` `0x${string}` | undefined ``

Implementation address to inspect, approve or use for derivation.

### confirmation

`ConfirmationPolicy | undefined`

Receipt confirmation policy.

### walletClient

`WalletClient | undefined`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

## Return Type

```ts
type Result = ReturnType<typeof useDeployHca>;
```

<MutationResult />

## Effect Atom

```ts
import { createDeployHcaMutationAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = createDeployHcaMutationAtom(sdk);
```

## Action

- [`deployHca`](/core/api/actions/hca/deploy-hca)
- [`sdk.hca.deployHca`](/sdk/api/hca/deploy-hca)
