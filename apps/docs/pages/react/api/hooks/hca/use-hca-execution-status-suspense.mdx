---
title: useHcaExecutionStatusSuspense
description: Reconcile a tracking reference
---

import ParametersGuide from "../../../../../shared/react/suspense-atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/suspense-atom-result.mdx";

# useHcaExecutionStatusSuspense

Reconcile a tracking reference Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useHcaExecutionStatusSuspense } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useHcaExecutionStatusSuspense } from "@ensforge/react";

function Component(parameters: Parameters<typeof useHcaExecutionStatusSuspense>[0]) {
  const result = useHcaExecutionStatusSuspense(parameters);
  return <p>{result.isWaiting ? "Loading" : "Result available"}</p>;
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

## Parameters

```ts
type Input = Parameters<typeof useHcaExecutionStatusSuspense>[0];
```

### submission

`HcaExecutionSubmission<unknown>`

Previously returned or explicitly recovered execution submission.

### execution

`ExecutionAdapter<unknown, unknown, unknown> | undefined`

Compatible execution adapter. Omit for direct owner execution when supported.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useHcaExecutionStatusSuspense>;
```

<ResultGuide />

## Effect Atom

```ts
import { getHcaExecutionStatusAtom } from "@ensforge/react/atoms";

const atom = getHcaExecutionStatusAtom(sdk, parameters, options);
```

## Action

- [`getHcaExecutionStatus`](/core/api/actions/hca/get-hca-execution-status)
- [`sdk.hca.getHcaExecutionStatus`](/sdk/api/hca/get-hca-execution-status)
