---
title: useHcaHookSuspense
description: Inspect the configured hook
---

import ParametersGuide from "../../../../../shared/react/suspense-atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/suspense-atom-result.mdx";
import { ReadActionDemo } from "../../../../../components/read-actions/read-action-demo.client.tsx";

# useHcaHookSuspense

Inspect the configured hook. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useHcaHookSuspense } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useHcaHookSuspense } from "@ensforge/react";

function Component() {
  const result = useHcaHookSuspense({
    hca: "0x1234567890123456789012345678901234567890",
  });

  return (
    <pre>
      {JSON.stringify(
        result.data,
        (_, value) => (typeof value === "bigint" ? value.toString() : value),
        2,
      )}
    </pre>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

<ReadActionDemo action="hca.getHcaHook" />

## Parameters

```ts
import type { UseEnsSuspenseAtomParameters } from "@ensforge/react";
import type { HcaReadParameters } from "@ensforge/sdk/hca";
```

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useHcaHookSuspense>;
```

<ResultGuide />

## Effect Atom

```ts
import { getHcaHookAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = getHcaHookAtom(sdk, parameters, options);
```

## Action

- [`getHcaHook`](/core/api/actions/hca/get-hca-hook)
- [`sdk.hca.getHcaHook`](/sdk/api/hca/get-hca-hook)
