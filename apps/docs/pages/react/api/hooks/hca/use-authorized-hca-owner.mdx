---
title: useAuthorizedHcaOwner
description: Resolve the authorized owner through the account interface
---

import ParametersGuide from "../../../../../shared/react/atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/atom-result.mdx";

# useAuthorizedHcaOwner

Resolve the authorized owner through the account interface Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useAuthorizedHcaOwner } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useAuthorizedHcaOwner } from "@ensforge/react";

function Component(parameters: Parameters<typeof useAuthorizedHcaOwner>[0]) {
  const result = useAuthorizedHcaOwner(parameters);
  return <p>{result.isWaiting ? "Loading" : "Result available"}</p>;
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

## Parameters

```ts
type Input = Parameters<typeof useAuthorizedHcaOwner>[0];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useAuthorizedHcaOwner>;
```

<ResultGuide />

## Suspense

Use [`useAuthorizedHcaOwnerSuspense`](/react/api/hooks/hca/use-authorized-hca-owner-suspense) beneath Suspense and an error boundary.

## Effect Atom

```ts
import { getAuthorizedHcaOwnerAtom } from "@ensforge/react/atoms";

const atom = getAuthorizedHcaOwnerAtom(sdk, parameters, options);
```

## Action

- [`getAuthorizedHcaOwner`](/core/api/actions/hca/get-authorized-hca-owner)
- [`sdk.hca.getAuthorizedHcaOwner`](/sdk/api/hca/get-authorized-hca-owner)
