---
title: usePredictHcaAddress
description: Predict the deterministic account address
---

import ParametersGuide from "../../../../../shared/react/atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/atom-result.mdx";
import { ReadActionDemo } from "../../../../../components/read-actions/read-action-demo.client.tsx";

# usePredictHcaAddress

Predict the deterministic account address. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { usePredictHcaAddress } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { usePredictHcaAddress } from "@ensforge/react";

function Component() {
  const result = usePredictHcaAddress({
    owner: "0x2345678901234567890123456789012345678901",
  });

  return (
    <pre>
      {JSON.stringify(
        result.data,
        (_, value) => (typeof value === "bigint" ? value.toString() : value),
        2,
      )}
    </pre>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

<ReadActionDemo action="hca.predictHcaAddress" />

## Parameters

```ts
import type { UseEnsAtomParameters } from "@ensforge/react";
import type { HcaDerivationParameters } from "@ensforge/sdk/hca";
```

### owner

`` `0x${string}` ``

Owner address used for account derivation or authorization.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

### implementation

`` `0x${string}` | undefined ``

Implementation address to inspect, approve or use for derivation.

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof usePredictHcaAddress>;
```

<ResultGuide />

## Suspense

Use [`usePredictHcaAddressSuspense`](/react/api/hooks/hca/use-predict-hca-address-suspense) beneath Suspense and an error boundary.

## Effect Atom

```ts
import { predictHcaAddressAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = predictHcaAddressAtom(sdk, parameters, options);
```

## Action

- [`predictHcaAddress`](/core/api/actions/hca/predict-hca-address)
- [`sdk.hca.predictHcaAddress`](/sdk/api/hca/predict-hca-address)
