---
title: useHcaSigningDomain
description: Read the account signing domain
---

import ParametersGuide from "../../../../../shared/react/atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/atom-result.mdx";
import { ReadActionDemo } from "../../../../../components/read-actions/read-action-demo.client.tsx";

# useHcaSigningDomain

Read the account signing domain. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useHcaSigningDomain } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useHcaSigningDomain } from "@ensforge/react";

function Component() {
  const result = useHcaSigningDomain({
    hca: "0x1234567890123456789012345678901234567890",
  });

  return (
    <pre>
      {JSON.stringify(
        result.data,
        (_, value) => (typeof value === "bigint" ? value.toString() : value),
        2,
      )}
    </pre>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

<ReadActionDemo action="hca.getHcaSigningDomain" />

## Parameters

```ts
import type { UseEnsAtomParameters } from "@ensforge/react";
import type { HcaReadParameters } from "@ensforge/sdk/hca";
```

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useHcaSigningDomain>;
```

<ResultGuide />

## Suspense

Use [`useHcaSigningDomainSuspense`](/react/api/hooks/hca/use-hca-signing-domain-suspense) beneath Suspense and an error boundary.

## Effect Atom

```ts
import { getHcaSigningDomainAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = getHcaSigningDomainAtom(sdk, parameters, options);
```

## Action

- [`getHcaSigningDomain`](/core/api/actions/hca/get-hca-signing-domain)
- [`sdk.hca.getHcaSigningDomain`](/sdk/api/hca/get-hca-signing-domain)
