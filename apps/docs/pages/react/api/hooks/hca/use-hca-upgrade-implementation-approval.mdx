---
title: useHcaUpgradeImplementationApproval
description: Read directional upgrade approval
---

import ParametersGuide from "../../../../../shared/react/atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/atom-result.mdx";

# useHcaUpgradeImplementationApproval

Read directional upgrade approval Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useHcaUpgradeImplementationApproval } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useHcaUpgradeImplementationApproval } from "@ensforge/react";

function Component(parameters: Parameters<typeof useHcaUpgradeImplementationApproval>[0]) {
  const result = useHcaUpgradeImplementationApproval(parameters);
  return <p>{result.isWaiting ? "Loading" : "Result available"}</p>;
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

## Parameters

```ts
type Input = Parameters<typeof useHcaUpgradeImplementationApproval>[0];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

### gate

`` `0x${string}` | undefined ``

Optional upgrade-gate address. Defaults to the deployment profile gate.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useHcaUpgradeImplementationApproval>;
```

<ResultGuide />

## Suspense

Use [`useHcaUpgradeImplementationApprovalSuspense`](/react/api/hooks/hca/use-hca-upgrade-implementation-approval-suspense) beneath Suspense and an error boundary.

## Effect Atom

```ts
import { getHcaUpgradeImplementationApprovalAtom } from "@ensforge/react/atoms";

const atom = getHcaUpgradeImplementationApprovalAtom(sdk, parameters, options);
```

## Action

- [`getHcaUpgradeImplementationApproval`](/core/api/actions/hca/get-hca-upgrade-implementation-approval)
- [`sdk.hca.getHcaUpgradeImplementationApproval`](/sdk/api/hca/get-hca-upgrade-implementation-approval)
