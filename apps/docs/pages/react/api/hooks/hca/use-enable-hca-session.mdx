---
title: useEnableHcaSession
description: Owner-authorize a constrained session
---

import MutationOptions from "../../../../../shared/react/mutation-options.mdx";
import MutationResult from "../../../../../shared/react/mutation-result.mdx";

# useEnableHcaSession

Owner-authorize a constrained session. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useEnableHcaSession } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useEnableHcaSession } from "@ensforge/react";
import type { EnableHcaSessionParameters } from "@ensforge/core/hca";

function Component({ session }: { session: EnableHcaSessionParameters }) {
  const mutation = useEnableHcaSession();

  return (
    <button
      disabled={mutation.isWaiting}
      onClick={() =>
        mutation.mutate({
          ...session,
        })
      }
    >
      Submit
    </button>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

<MutationOptions />

## Mutation Parameters

```ts
import type { EnableHcaSessionParameters } from "@ensforge/sdk/hca";
```

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### permissionId

`` `0x${string}` ``

Permission identifier of the prepared or enabled session.

### sessionKey

`` `0x${string}` ``

Address of the session signer. The private key is not passed to the action.

### validUntil

`number`

Session expiry as a Unix timestamp in seconds.

### resolver

`` `0x${string}` ``

Resolver address used by the registration or session policy.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

### walletClient

`WalletClient | undefined`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

## Return Type

```ts
type Result = ReturnType<typeof useEnableHcaSession>;
```

<MutationResult />

## Effect Atom

```ts
import { createEnableHcaSessionMutationAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = createEnableHcaSessionMutationAtom(sdk);
```

## Action

- [`enableHcaSession`](/core/api/actions/hca/enable-hca-session)
- [`sdk.hca.enableHcaSession`](/sdk/api/hca/enable-hca-session)
