---
title: useHcaDepositSuspense
description: Read the EntryPoint deposit balance
---

import ParametersGuide from "../../../../../shared/react/suspense-atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/suspense-atom-result.mdx";
import { ReadActionDemo } from "../../../../../components/read-actions/read-action-demo.client.tsx";

# useHcaDepositSuspense

Read the EntryPoint deposit balance. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useHcaDepositSuspense } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useHcaDepositSuspense } from "@ensforge/react";

function Component() {
  const result = useHcaDepositSuspense({
    hca: "0x1234567890123456789012345678901234567890",
  });

  return (
    <pre>
      {JSON.stringify(
        result.data,
        (_, value) => (typeof value === "bigint" ? value.toString() : value),
        2,
      )}
    </pre>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

<ReadActionDemo action="hca.getHcaDeposit" />

## Parameters

```ts
import type { UseEnsSuspenseAtomParameters } from "@ensforge/react";
import type { HcaReadParameters } from "@ensforge/sdk/hca";
```

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useHcaDepositSuspense>;
```

<ResultGuide />

## Effect Atom

```ts
import { getHcaDepositAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = getHcaDepositAtom(sdk, parameters, options);
```

## Action

- [`getHcaDeposit`](/core/api/actions/hca/get-hca-deposit)
- [`sdk.hca.getHcaDeposit`](/sdk/api/hca/get-hca-deposit)
