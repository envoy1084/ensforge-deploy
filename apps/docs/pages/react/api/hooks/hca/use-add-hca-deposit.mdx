---
title: useAddHcaDeposit
description: Deposit native funds into EntryPoint
---

import MutationOptions from "../../../../../shared/react/mutation-options.mdx";
import MutationResult from "../../../../../shared/react/mutation-result.mdx";

# useAddHcaDeposit

Deposit native funds into EntryPoint. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useAddHcaDeposit } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useAddHcaDeposit } from "@ensforge/react";

function Component() {
  const mutation = useAddHcaDeposit();

  return (
    <button
      disabled={mutation.isWaiting}
      onClick={() =>
        mutation.mutate({
          hca: "0x1234567890123456789012345678901234567890",
          amount: 1_000_000_000_000_000n,
        })
      }
    >
      Submit
    </button>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

<MutationOptions />

## Mutation Parameters

```ts
import type { HcaManagementParameters } from "@ensforge/sdk/hca";
```

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### amount

`bigint`

Native token amount in wei.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

### confirmation

`ConfirmationPolicy | undefined`

Receipt confirmation policy.

### walletClient

`WalletClient | undefined`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

## Return Type

```ts
type Result = ReturnType<typeof useAddHcaDeposit>;
```

<MutationResult />

## Effect Atom

```ts
import { createAddHcaDepositMutationAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = createAddHcaDepositMutationAtom(sdk);
```

## Action

- [`addHcaDeposit`](/core/api/actions/hca/add-hca-deposit)
- [`sdk.hca.addHcaDeposit`](/sdk/api/hca/add-hca-deposit)
