---
title: useHcaUpgradeImplementationApprovalSuspense
description: Read directional upgrade approval
---

import ParametersGuide from "../../../../../shared/react/suspense-atom-parameters.mdx";
import ResultGuide from "../../../../../shared/react/suspense-atom-result.mdx";

# useHcaUpgradeImplementationApprovalSuspense

Read directional upgrade approval. Uses the SDK from `EnsforgeProvider`.

## Import

```tsx
import { useHcaUpgradeImplementationApprovalSuspense } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useHcaUpgradeImplementationApprovalSuspense } from "@ensforge/react";

function Component() {
  const result = useHcaUpgradeImplementationApprovalSuspense({
    implementation: "0x5678901234567890123456789012345678901234",
  });

  return (
    <pre>
      {JSON.stringify(
        result.data,
        (_, value) => (typeof value === "bigint" ? value.toString() : value),
        2,
      )}
    </pre>
  );
}
```

```tsx [provider.tsx]
// [!include ~/snippets/hca/provider.tsx]
```

:::

## Parameters

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

### gate

`` `0x${string}` | undefined ``

Optional upgrade-gate address. Defaults to the deployment profile gate.

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

<ParametersGuide />

## Return Type

```ts
type Result = ReturnType<typeof useHcaUpgradeImplementationApprovalSuspense>;
```

<ResultGuide />

## Effect Atom

```ts
import { getHcaUpgradeImplementationApprovalAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = getHcaUpgradeImplementationApprovalAtom(sdk, parameters, options);
```

## Action

- [`getHcaUpgradeImplementationApproval`](/core/api/actions/hca/get-hca-upgrade-implementation-approval)
- [`sdk.hca.getHcaUpgradeImplementationApproval`](/sdk/api/hca/get-hca-upgrade-implementation-approval)
