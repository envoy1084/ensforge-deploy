---
title: isHcaImplementationTrusted
description: Check trusted implementation membership.
---

import EffectGuide from "../../../../shared/sdk/effect.mdx";
import ErrorGuide from "../../../../shared/sdk/error.mdx";

# isHcaImplementationTrusted

Check trusted implementation membership.

## Import

```ts
import { Ensforge } from "@ensforge/sdk";
```

## Usage

:::code-group

```ts [index.ts]
import { sdk } from "./client";

type Input = Parameters<typeof sdk.hca.isHcaImplementationTrusted>[0];
const parameters: Input = {
  implementation: "0x0000000000000000000000000000000000000001",
};

const result = await sdk.hca.isHcaImplementationTrusted(parameters);
```

```ts [client.ts]
// [!include ~/snippets/hca/client.ts]
```

:::

## Parameters

```ts
type Input = Parameters<typeof sdk.hca.isHcaImplementationTrusted>[0];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof sdk.hca.isHcaImplementationTrusted>>;
```

Returns `boolean`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = sdk.hca.isHcaImplementationTrusted.effect(parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = sdk.hca.isHcaImplementationTrusted.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`isHcaImplementationTrusted`](/core/api/actions/hca/is-hca-implementation-trusted)
- [Guide](/core/guides/hca)
