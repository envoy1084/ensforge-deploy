---
title: cancelHcaRegistration
description: Cancel eligible local progression.
---

import EffectGuide from "../../../../shared/sdk/effect.mdx";
import ErrorGuide from "../../../../shared/sdk/error.mdx";

# cancelHcaRegistration

Cancel eligible local progression.

## Import

```ts
import { Ensforge } from "@ensforge/sdk";
```

## Usage

:::code-group

```ts [index.ts]
import { sdk } from "./client";

type Input = Parameters<typeof sdk.hca.cancelHcaRegistration>[0];
declare const parameters: Input;

const result = await sdk.hca.cancelHcaRegistration(parameters);
```

```ts [client.ts]
// [!include ~/snippets/hca/client.ts]
```

:::

## Parameters

```ts
type Input = Parameters<typeof sdk.hca.cancelHcaRegistration>[0];
```

### id

`string`

Saved operation identifier. Start registration can generate it from matching unfinished work.

### storage

`WorkflowStorage | HcaRegistrationStorage | undefined`

Optional storage override. Defaults to config storage.

### execution

`HcaRegistrationExecution | undefined`

Compatible execution adapter. Omit for direct owner execution when supported.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof sdk.hca.cancelHcaRegistration>>;
```

Common result properties are shown below. Narrow discriminated unions before accessing status-specific fields.

- `authorization`: ``{ readonly kind: "owner"; } | { readonly kind: "session"; readonly permissionId: `0x${string}`; readonly enableTransactionHash: `0x${string}`; }``.
- `id`: `string`.
- `chainId`: `number`.
- `owner`: `` `0x${string}` ``.
- `profileId`: `string`.
- `salt`: `bigint`.
- `hca`: `` `0x${string}` ``.
- `registration`: `Result["registration"]`.
- `schemaVersion`: `1`.
- `route`: `{ readonly kind: "owner"; } | { readonly kind: "adapter"; readonly id: string; readonly instanceId: string; }`.
- `revision`: `number`.
- `limits`: `Result["limits"]`.
- `progress`: `Result["progress"]`.
- `createdAt`: `bigint`.
- `updatedAt`: `bigint`.
- `signerReference`: `string | undefined`.
- `commitmentSubmission`: `Result["commitmentSubmission"]`.
- `registrationSubmission`: `Result["registrationSubmission"]`.

Registration results contain a commitment secret. Do not log or render the whole operation. Use [remote progress](/hca/remote) for a public response.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = sdk.hca.cancelHcaRegistration.effect(parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`cancelHcaRegistration`](/core/api/actions/hca/cancel-hca-registration)
- [Guide](/core/guides/hca-registration)
