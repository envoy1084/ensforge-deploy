---
title: enableHcaSession
description: Owner-authorize a constrained session.
---

import EffectGuide from "../../../../shared/sdk/effect.mdx";
import ErrorGuide from "../../../../shared/sdk/error.mdx";

# enableHcaSession

Owner-authorize a constrained session.

## Import

```ts
import { Ensforge } from "@ensforge/sdk";
```

## Usage

:::code-group

```ts [index.ts]
import { sdk } from "./client";

type Input = Parameters<typeof sdk.hca.enableHcaSession>[0];
declare const parameters: Input;

const result = await sdk.hca.enableHcaSession(parameters);
```

```ts [client.ts]
// [!include ~/snippets/hca/client.ts]
```

:::

## Parameters

```ts
type Input = Parameters<typeof sdk.hca.enableHcaSession>[0];
```

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

### permissionId

`` `0x${string}` ``

Permission identifier of the prepared or enabled session.

### sessionKey

`` `0x${string}` ``

Address of the session signer. The private key is not passed to the action.

### validUntil

`number`

Session expiry as a Unix timestamp in seconds.

### resolver

`` `0x${string}` ``

Resolver address used by the registration or session policy.

### walletClient

`Input["walletClient"]`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof sdk.hca.enableHcaSession>>;
```

Returns `HcaTransactionSubmission`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = sdk.hca.enableHcaSession.effect(parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Call

```ts
const intent = sdk.hca.enableHcaSession.call(parameters);
```

Preparing an intent does not submit a transaction.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`enableHcaSession`](/core/api/actions/hca/enable-hca-session)
- [Guide](/core/guides/hca)
