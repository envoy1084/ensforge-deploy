---
title: getTrustedHcaRoles
description: Read resource roles on a trusted HCA set.
---

import EffectGuide from "../../../../../shared/sdk/effect.mdx";
import ErrorGuide from "../../../../../shared/sdk/error.mdx";
import RequestGuide from "../../../../../shared/sdk/request.mdx";

# getTrustedHcaRoles

Read resource roles on a trusted HCA set.

## Import

```ts
import { Ensforge } from "@ensforge/sdk";
```

## Usage

:::code-group

```ts [index.ts]
import { sdk } from "./client";

const result = await sdk.hca.admin.getTrustedHcaRoles({
  assignee: "0x2345678901234567890123456789012345678901",
});
```

```ts [client.ts]
// [!include ~/snippets/hca/client.ts]
```

:::

This targets HCA governance contracts. It does not transfer or renounce the immutable account owner.

## Parameters

### assignee

`` `0x${string}` ``

Address whose resource roles are read, granted or revoked.

### resource

`bigint | undefined`

Resource identifier for the role operation.

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

## Return Type

```ts
type GetTrustedHcaRolesResult = Awaited<ReturnType<typeof sdk.hca.admin.getTrustedHcaRoles>>;
```

Returns `bigint`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = sdk.hca.admin.getTrustedHcaRoles.effect({
  assignee: "0x2345678901234567890123456789012345678901",
});

type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;

const result = await Effect.runPromise(program);
```

## Request

<RequestGuide />

```ts
const request = sdk.hca.admin.getTrustedHcaRoles.request({
  assignee: "0x2345678901234567890123456789012345678901",
});
```

## Error

<ErrorGuide />

## Action

- [`getTrustedHcaRoles`](/core/api/actions/hca/admin/get-trusted-hca-roles)
- [Guide](/core/guides/hca)
