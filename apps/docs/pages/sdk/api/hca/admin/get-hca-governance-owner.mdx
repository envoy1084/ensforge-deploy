---
title: getHcaGovernanceOwner
description: Read the owner of a supported HCA governance contract.
---

import EffectGuide from "../../../../../shared/sdk/effect.mdx";
import ErrorGuide from "../../../../../shared/sdk/error.mdx";

# getHcaGovernanceOwner

Read the owner of a supported HCA governance contract.

## Import

```ts
import { Ensforge } from "@ensforge/sdk";
```

## Usage

:::code-group

```ts [index.ts]
import { sdk } from "./client";

type Input = Parameters<typeof sdk.hca.admin.getHcaGovernanceOwner>[0];
declare const parameters: Input;

const result = await sdk.hca.admin.getHcaGovernanceOwner(parameters);
```

```ts [client.ts]
// [!include ~/snippets/hca/client.ts]
```

:::

This targets HCA governance contracts. It does not transfer or renounce the immutable account owner.

## Parameters

```ts
type Input = Parameters<typeof sdk.hca.admin.getHcaGovernanceOwner>[0];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### target

`HcaGovernanceTarget`

Governance contract to inspect or modify.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof sdk.hca.admin.getHcaGovernanceOwner>>;
```

Returns `` `0x${string}` ``.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = sdk.hca.admin.getHcaGovernanceOwner.effect(parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = sdk.hca.admin.getHcaGovernanceOwner.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`getHcaGovernanceOwner`](/core/api/actions/hca/admin/get-hca-governance-owner)
- [Guide](/core/guides/hca)
