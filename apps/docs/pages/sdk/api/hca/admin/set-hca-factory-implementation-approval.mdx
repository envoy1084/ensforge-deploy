---
title: setHcaFactoryImplementationApproval
description: Set an implementation approval on the HCA factory.
---

import EffectGuide from "../../../../../shared/sdk/effect.mdx";
import ErrorGuide from "../../../../../shared/sdk/error.mdx";

# setHcaFactoryImplementationApproval

Set an implementation approval on the HCA factory.

## Import

```ts
import { Ensforge } from "@ensforge/sdk";
```

## Usage

:::code-group

```ts [index.ts]
import { sdk } from "./client";

const result = await sdk.hca.admin.setHcaFactoryImplementationApproval({
  implementation: "0x5678901234567890123456789012345678901234",
  approved: true,
});
```

```ts [client.ts]
// [!include ~/snippets/hca/client.ts]
```

:::

This targets HCA governance contracts. It does not transfer or renounce the immutable account owner.

## Parameters

```ts
import type { HcaAdminWriteParameters } from "@ensforge/sdk/hca/admin";
```

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

### approved

`boolean`

Whether to approve the selected implementation.

### confirmation

`ConfirmationPolicy | undefined`

Receipt confirmation policy.

### walletClient

`WalletClient | undefined`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

## Return Type

```ts
import type { HcaManagementResult } from "@ensforge/sdk/hca";
```

| Property  | Type                         | Description                                    |
| --------- | ---------------------------- | ---------------------------------------------- |
| `hash`    | `` `0x${string}` ``          | Submitted transaction hash.                    |
| `receipt` | `TransactionReceipt \| null` | Confirmed transaction receipt, when requested. |

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = sdk.hca.admin.setHcaFactoryImplementationApproval.effect({
  implementation: "0x5678901234567890123456789012345678901234",
  approved: true,
});

type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;

const result = await Effect.runPromise(program);
```

## Error

<ErrorGuide />

## Action

- [`setHcaFactoryImplementationApproval`](/core/api/actions/hca/admin/set-hca-factory-implementation-approval)
- [Guide](/core/guides/hca)
