---
title: renounceHcaGovernanceOwnership
description: Renounce ownership of a supported HCA governance contract.
---

import EffectGuide from "../../../../../shared/sdk/effect.mdx";
import ErrorGuide from "../../../../../shared/sdk/error.mdx";

# renounceHcaGovernanceOwnership

Renounce ownership of a supported HCA governance contract.

## Import

```ts
import { Ensforge } from "@ensforge/sdk";
```

## Usage

:::code-group

```ts [index.ts]
import { sdk } from "./client";

type Input = Parameters<typeof sdk.hca.admin.renounceHcaGovernanceOwnership>[0];
declare const parameters: Input;

const result = await sdk.hca.admin.renounceHcaGovernanceOwnership(parameters);
```

```ts [client.ts]
// [!include ~/snippets/hca/client.ts]
```

:::

This targets HCA governance contracts. It does not transfer or renounce the immutable account owner.

## Parameters

```ts
type Input = Parameters<typeof sdk.hca.admin.renounceHcaGovernanceOwnership>[0];
```

### confirmation

`{ readonly type: "submitted"; } | { readonly type: "confirmed"; readonly timeout?: number | undefined; readonly confirmations?: number | undefined; }`

Receipt confirmation policy.

### walletClient

`Input["walletClient"]`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

### target

`HcaGovernanceTarget`

Governance contract to inspect or modify.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof sdk.hca.admin.renounceHcaGovernanceOwnership>>;
```

Returns `HcaManagementResult`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = sdk.hca.admin.renounceHcaGovernanceOwnership.effect(parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`renounceHcaGovernanceOwnership`](/core/api/actions/hca/admin/renounce-hca-governance-ownership)
- [Guide](/core/guides/hca)
