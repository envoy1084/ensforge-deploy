---
title: getHcaValidators
description: Inspect validators with pagination.
---

import EffectGuide from "../../../../shared/sdk/effect.mdx";
import ErrorGuide from "../../../../shared/sdk/error.mdx";

# getHcaValidators

Inspect validators with pagination.

## Import

```ts
import { Ensforge } from "@ensforge/sdk";
```

## Usage

:::code-group

```ts [index.ts]
import { sdk } from "./client";

type Input = Parameters<typeof sdk.hca.getHcaValidators>[0];
const parameters: Input = {
  hca: "0x0000000000000000000000000000000000000001",
};

const result = await sdk.hca.getHcaValidators(parameters);
```

```ts [client.ts]
// [!include ~/snippets/hca/client.ts]
```

:::

## Parameters

```ts
type Input = Parameters<typeof sdk.hca.getHcaValidators>[0];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### cursor

`` `0x${string}` | undefined ``

Starting address for module pagination.

### size

`bigint | undefined`

Maximum number of module entries to return.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof sdk.hca.getHcaValidators>>;
```

Returns `HcaModulePage`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = sdk.hca.getHcaValidators.effect(parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = sdk.hca.getHcaValidators.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`getHcaValidators`](/core/api/actions/hca/get-hca-validators)
- [Guide](/core/guides/hca)
