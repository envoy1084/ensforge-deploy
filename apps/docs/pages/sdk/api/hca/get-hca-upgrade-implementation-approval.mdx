---
title: getHcaUpgradeImplementationApproval
description: Read directional upgrade approval.
---

import EffectGuide from "../../../../shared/sdk/effect.mdx";
import ErrorGuide from "../../../../shared/sdk/error.mdx";

# getHcaUpgradeImplementationApproval

Read directional upgrade approval.

## Import

```ts
import { Ensforge } from "@ensforge/sdk";
```

## Usage

:::code-group

```ts [index.ts]
import { sdk } from "./client";

type Input = Parameters<typeof sdk.hca.getHcaUpgradeImplementationApproval>[0];
const parameters: Input = {
  implementation: "0x0000000000000000000000000000000000000001",
};

const result = await sdk.hca.getHcaUpgradeImplementationApproval(parameters);
```

```ts [client.ts]
// [!include ~/snippets/hca/client.ts]
```

:::

## Parameters

```ts
type Input = Parameters<typeof sdk.hca.getHcaUpgradeImplementationApproval>[0];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

### gate

`` `0x${string}` | undefined ``

Optional upgrade-gate address. Defaults to the deployment profile gate.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof sdk.hca.getHcaUpgradeImplementationApproval>>;
```

Returns `boolean`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = sdk.hca.getHcaUpgradeImplementationApproval.effect(parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = sdk.hca.getHcaUpgradeImplementationApproval.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`getHcaUpgradeImplementationApproval`](/core/api/actions/hca/get-hca-upgrade-implementation-approval)
- [Guide](/core/guides/hca)
