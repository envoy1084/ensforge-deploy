---
title: reconcileWorkflowSubmission
description: Attach a recovered transaction hash after verifying the saved submission intent.
---

import EffectGuide from "../../../../shared/sdk/effect.mdx";
import ErrorGuide from "../../../../shared/sdk/error.mdx";

# reconcileWorkflowSubmission

Attach a recovered transaction hash after verifying the saved submission intent.

## Import

```ts
import { Ensforge } from "@ensforge/sdk";
```

## Usage

:::code-group

```ts [index.ts]
import { sdk } from "./client";

type Input = Parameters<typeof sdk.workflows.reconcileWorkflowSubmission>[0];
declare const parameters: Input;

const result = await sdk.workflows.reconcileWorkflowSubmission(parameters);
```

```ts [client.ts]
// [!include ~/snippets/hca/client.ts]
```

:::

## Parameters

```ts
type Input = Parameters<typeof sdk.workflows.reconcileWorkflowSubmission>[0];
```

### walletClient

`Input["walletClient"]`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

### workflowId

`string`

Existing saved workflow instance ID.

### submissionId

`string`

Submission key from getWorkflow().submissions.

### transactionHash

`` `0x${string}` ``

Recovered on-chain transaction hash to verify.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof sdk.workflows.reconcileWorkflowSubmission>>;
```

Returns `void`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = sdk.workflows.reconcileWorkflowSubmission.effect(parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`reconcileWorkflowSubmission`](/core/api/actions/workflows/reconcile-workflow-submission)
- [Guide](/core/guides/workflow-storage)
