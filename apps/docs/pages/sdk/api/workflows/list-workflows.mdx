---
title: listWorkflows
description: List workflows for the configured chain and wallet account.
---

import EffectGuide from "../../../../shared/sdk/effect.mdx";
import ErrorGuide from "../../../../shared/sdk/error.mdx";

# listWorkflows

List workflows for the configured chain and wallet account.

## Import

```ts
import { Ensforge } from "@ensforge/sdk";
```

## Usage

:::code-group

```ts [index.ts]
import { sdk } from "./client";

type Input = Parameters<typeof sdk.workflows.listWorkflows>[0];
declare const parameters: Input;

const result = await sdk.workflows.listWorkflows(parameters);
```

```ts [client.ts]
// [!include ~/snippets/hca/client.ts]
```

:::

## Parameters

```ts
type Input = Parameters<typeof sdk.workflows.listWorkflows>[0];
```

### walletClient

`Input["walletClient"]`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

### after

`string | undefined`

Exclusive pagination cursor from the previous response.

### limit

`number | undefined`

Page size between 1 and 100.

### status

`"pending" | "completed" | undefined`

Optional pending/completed workflow filter.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof sdk.workflows.listWorkflows>>;
```

Returns `{ readonly workflows: readonly { readonly workflowId: string; readonly operation: string; readonly status: "pending" | "completed"; }[]; readonly nextCursor: string | null; }`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = sdk.workflows.listWorkflows.effect(parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`listWorkflows`](/core/api/actions/workflows/list-workflows)
- [Guide](/core/guides/workflow-storage)
