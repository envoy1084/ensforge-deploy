---
title: cancelHcaRegistration
description: Cancel eligible local progression.
---

import EffectGuide from "../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../shared/core/error.mdx";

# cancelHcaRegistration

Cancel eligible local progression.

## Import

```ts
import { cancelHcaRegistration } from "@ensforge/core/hca";
```

## Usage

:::code-group

```ts [index.ts]
import { cancelHcaRegistration } from "@ensforge/core/hca";
import { config } from "./config";

const result = await cancelHcaRegistration(config, {
  id: "registration-instance-id",
});
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

## Parameters

```ts
import type { GetHcaRegistrationParameters } from "@ensforge/core/hca";
```

### id

`string`

Saved operation identifier. Start registration can generate it from matching unfinished work.

### storage

`WorkflowStorage | HcaRegistrationStorage | undefined`

Optional storage override. Defaults to config storage.

### execution

`HcaRegistrationExecution | undefined`

Compatible execution adapter. Omit for direct owner execution when supported.

## Return Type

```ts
type CancelHcaRegistrationResult = Awaited<ReturnType<typeof cancelHcaRegistration>>;
```

Narrow discriminated unions before accessing fields specific to a status or execution route.

| Property                 | Type                                                                                                                                                  |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `authorization`          | ``{ readonly kind: "owner"; } \| { readonly kind: "session"; readonly permissionId: `0x${string}`; readonly enableTransactionHash: `0x${string}`; }`` |
| `id`                     | `string`                                                                                                                                              |
| `chainId`                | `number`                                                                                                                                              |
| `owner`                  | `` `0x${string}` ``                                                                                                                                   |
| `profileId`              | `string`                                                                                                                                              |
| `salt`                   | `bigint`                                                                                                                                              |
| `hca`                    | `` `0x${string}` ``                                                                                                                                   |
| `registration`           | `CancelHcaRegistrationResult["registration"]`                                                                                                         |
| `schemaVersion`          | `1`                                                                                                                                                   |
| `route`                  | `{ readonly kind: "owner"; } \| { readonly kind: "adapter"; readonly id: string; readonly instanceId: string; }`                                      |
| `revision`               | `number`                                                                                                                                              |
| `limits`                 | `CancelHcaRegistrationResult["limits"]`                                                                                                               |
| `progress`               | `CancelHcaRegistrationResult["progress"]`                                                                                                             |
| `createdAt`              | `bigint`                                                                                                                                              |
| `updatedAt`              | `bigint`                                                                                                                                              |
| `signerReference`        | `string \| undefined`                                                                                                                                 |
| `commitmentSubmission`   | `CancelHcaRegistrationResult["commitmentSubmission"]`                                                                                                 |
| `registrationSubmission` | `CancelHcaRegistrationResult["registrationSubmission"]`                                                                                               |

Registration results contain a commitment secret. Do not log or render the whole operation. Use [remote progress](/hca/remote) for a public response.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = cancelHcaRegistration.effect(config, {
  id: "registration-instance-id",
});

type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;

const result = await Effect.runPromise(program);
```

## Error

<ErrorGuide />

## Related

- [`cancelHcaRegistration`](/sdk/api/hca/cancel-hca-registration)
- [Guide](/core/guides/hca-registration)
