---
title: getHcaSigningDomain
description: Read the account signing domain.
---

import { ReadActionDemo } from "../../../../../components/read-actions/read-action-demo.client.tsx";
import EffectGuide from "../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../shared/core/error.mdx";

# getHcaSigningDomain

Read the account signing domain.

## Import

```ts
import { getHcaSigningDomain } from "@ensforge/core/hca";
```

## Usage

:::code-group

```ts [index.ts]
import { getHcaSigningDomain } from "@ensforge/core/hca";
import { config } from "./config";

type Input = Parameters<typeof getHcaSigningDomain>[1];
const parameters: Input = {
  hca: "0x0000000000000000000000000000000000000001",
};

const result = await getHcaSigningDomain(config, parameters);
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

<ReadActionDemo action="hca.getHcaSigningDomain" />

## Parameters

```ts
type Input = Parameters<typeof getHcaSigningDomain>[1];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof getHcaSigningDomain>>;
```

Returns `HcaSigningDomain`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = getHcaSigningDomain.effect(config, parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = getHcaSigningDomain.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`getHcaSigningDomain`](/sdk/api/hca/get-hca-signing-domain)
- [Guide](/core/guides/hca)
