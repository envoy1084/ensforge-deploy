---
title: predictHcaAddress
description: Predict the deterministic account address.
---

import { ReadActionDemo } from "../../../../../components/read-actions/read-action-demo.client.tsx";
import EffectGuide from "../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../shared/core/error.mdx";

# predictHcaAddress

Predict the deterministic account address.

## Import

```ts
import { predictHcaAddress } from "@ensforge/core/hca";
```

## Usage

:::code-group

```ts [index.ts]
import { predictHcaAddress } from "@ensforge/core/hca";
import { config } from "./config";

type Input = Parameters<typeof predictHcaAddress>[1];
const parameters: Input = {
  owner: "0x0000000000000000000000000000000000000001",
};

const result = await predictHcaAddress(config, parameters);
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

<ReadActionDemo action="hca.predictHcaAddress" />

## Parameters

```ts
type Input = Parameters<typeof predictHcaAddress>[1];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### owner

`` `0x${string}` ``

Owner address used for account derivation or authorization.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

### implementation

`` `0x${string}` | undefined ``

Implementation address to inspect, approve or use for derivation.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof predictHcaAddress>>;
```

Returns `` `0x${string}` ``.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = predictHcaAddress.effect(config, parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = predictHcaAddress.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`predictHcaAddress`](/sdk/api/hca/predict-hca-address)
- [Guide](/core/guides/hca)
