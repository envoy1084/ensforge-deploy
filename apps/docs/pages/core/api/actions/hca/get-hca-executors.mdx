---
title: getHcaExecutors
description: Inspect executors with pagination.
---

import EffectGuide from "../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../shared/core/error.mdx";

# getHcaExecutors

Inspect executors with pagination.

## Import

```ts
import { getHcaExecutors } from "@ensforge/core/hca";
```

## Usage

:::code-group

```ts [index.ts]
import { getHcaExecutors } from "@ensforge/core/hca";
import { config } from "./config";

type Input = Parameters<typeof getHcaExecutors>[1];
const parameters: Input = {
  hca: "0x0000000000000000000000000000000000000001",
};

const result = await getHcaExecutors(config, parameters);
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

## Parameters

```ts
type Input = Parameters<typeof getHcaExecutors>[1];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### cursor

`` `0x${string}` | undefined ``

Starting address for module pagination.

### size

`bigint | undefined`

Maximum number of module entries to return.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof getHcaExecutors>>;
```

Returns `HcaModulePage`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = getHcaExecutors.effect(config, parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = getHcaExecutors.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`getHcaExecutors`](/sdk/api/hca/get-hca-executors)
- [Guide](/core/guides/hca)
