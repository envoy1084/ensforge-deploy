---
title: getHcaUpgradeEligibility
description: Check the candidate implementation and both upgrade gates.
---

import EffectGuide from "../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../shared/core/error.mdx";
import RequestGuide from "../../../../../shared/core/request.mdx";

# getHcaUpgradeEligibility

Check the candidate implementation and both upgrade gates.

## Import

```ts
import { getHcaUpgradeEligibility } from "@ensforge/core/hca";
```

## Usage

:::code-group

```ts [index.ts]
import { getHcaUpgradeEligibility } from "@ensforge/core/hca";
import { config } from "./config";

const result = await getHcaUpgradeEligibility(config, {
  hca: "0x1234567890123456789012345678901234567890",
  implementation: "0x5678901234567890123456789012345678901234",
});
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

## Parameters

```ts
import type { HcaReadParameters } from "@ensforge/core/hca";
```

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

## Return Type

```ts
import type { HcaUpgradeEligibility } from "@ensforge/core/hca";
```

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = getHcaUpgradeEligibility.effect(config, {
  hca: "0x1234567890123456789012345678901234567890",
  implementation: "0x5678901234567890123456789012345678901234",
});

type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;

const result = await Effect.runPromise(program);
```

## Request

<RequestGuide />

```ts
const request = getHcaUpgradeEligibility.request({
  hca: "0x1234567890123456789012345678901234567890",
  implementation: "0x5678901234567890123456789012345678901234",
});
```

## Error

<ErrorGuide />

## Related

- [`getHcaUpgradeEligibility`](/sdk/api/hca/get-hca-upgrade-eligibility)
- [Guide](/core/guides/hca)
