---
title: setHcaUpgradeImplementationApproval
description: Set a directional implementation approval on the upgrade gate.
---

import EffectGuide from "../../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../../shared/core/error.mdx";

# setHcaUpgradeImplementationApproval

Set a directional implementation approval on the upgrade gate.

## Import

```ts
import { setHcaUpgradeImplementationApproval } from "@ensforge/core/hca/admin";
```

## Usage

:::code-group

```ts [index.ts]
import { setHcaUpgradeImplementationApproval } from "@ensforge/core/hca/admin";
import { config } from "./config";

type Input = Parameters<typeof setHcaUpgradeImplementationApproval>[1];
declare const parameters: Input;

const result = await setHcaUpgradeImplementationApproval(config, parameters);
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

This targets HCA governance contracts. It does not transfer or renounce the immutable account owner.

## Parameters

```ts
type Input = Parameters<typeof setHcaUpgradeImplementationApproval>[1];
```

### confirmation

`{ readonly type: "submitted"; } | { readonly type: "confirmed"; readonly timeout?: number | undefined; readonly confirmations?: number | undefined; }`

Receipt confirmation policy.

### walletClient

`Input["walletClient"]`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

### approved

`boolean`

Whether to approve the selected implementation.

### gate

`` `0x${string}` | undefined ``

Optional upgrade-gate address. Defaults to the deployment profile gate.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof setHcaUpgradeImplementationApproval>>;
```

Returns `HcaManagementResult`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = setHcaUpgradeImplementationApproval.effect(config, parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`setHcaUpgradeImplementationApproval`](/sdk/api/hca/admin/set-hca-upgrade-implementation-approval)
- [Guide](/core/guides/hca)
