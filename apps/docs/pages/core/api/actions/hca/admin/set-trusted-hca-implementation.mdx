---
title: setTrustedHcaImplementation
description: Update trusted implementation membership.
---

import EffectGuide from "../../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../../shared/core/error.mdx";

# setTrustedHcaImplementation

Update trusted implementation membership.

## Import

```ts
import { setTrustedHcaImplementation } from "@ensforge/core/hca/admin";
```

## Usage

:::code-group

```ts [index.ts]
import { setTrustedHcaImplementation } from "@ensforge/core/hca/admin";
import { config } from "./config";

const result = await setTrustedHcaImplementation(config, {
  implementation: "0x5678901234567890123456789012345678901234",
  approved: true,
});
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

This targets HCA governance contracts. It does not transfer or renounce the immutable account owner.

## Parameters

```ts
import type { HcaAdminWriteParameters } from "@ensforge/core/hca/admin";
```

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

### approved

`boolean`

Whether to approve the selected implementation.

### confirmation

`ConfirmationPolicy | undefined`

Receipt confirmation policy.

### walletClient

`WalletClient | undefined`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

## Return Type

```ts
import type { HcaManagementResult } from "@ensforge/core/hca";
```

| Property  | Type                         | Description                                    |
| --------- | ---------------------------- | ---------------------------------------------- |
| `hash`    | `` `0x${string}` ``          | Submitted transaction hash.                    |
| `receipt` | `TransactionReceipt \| null` | Confirmed transaction receipt, when requested. |

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = setTrustedHcaImplementation.effect(config, {
  implementation: "0x5678901234567890123456789012345678901234",
  approved: true,
});

type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;

const result = await Effect.runPromise(program);
```

## Error

<ErrorGuide />

## Related

- [`setTrustedHcaImplementation`](/sdk/api/hca/admin/set-trusted-hca-implementation)
- [Guide](/core/guides/hca)
