---
title: getTrustedHcaRoles
description: Read resource roles on a trusted HCA set.
---

import EffectGuide from "../../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../../shared/core/error.mdx";

# getTrustedHcaRoles

Read resource roles on a trusted HCA set.

## Import

```ts
import { getTrustedHcaRoles } from "@ensforge/core/hca/admin";
```

## Usage

:::code-group

```ts [index.ts]
import { getTrustedHcaRoles } from "@ensforge/core/hca/admin";
import { config } from "./config";

type Input = Parameters<typeof getTrustedHcaRoles>[1];
declare const parameters: Input;

const result = await getTrustedHcaRoles(config, parameters);
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

This targets HCA governance contracts. It does not transfer or renounce the immutable account owner.

## Parameters

```ts
type Input = Parameters<typeof getTrustedHcaRoles>[1];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### assignee

`` `0x${string}` ``

Address whose resource roles are read, granted or revoked.

### resource

`bigint | undefined`

Resource identifier for the role operation.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof getTrustedHcaRoles>>;
```

Returns `bigint`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = getTrustedHcaRoles.effect(config, parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = getTrustedHcaRoles.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`getTrustedHcaRoles`](/sdk/api/hca/admin/get-trusted-hca-roles)
- [Guide](/core/guides/hca)
