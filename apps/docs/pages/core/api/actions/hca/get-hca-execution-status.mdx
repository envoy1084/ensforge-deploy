---
title: getHcaExecutionStatus
description: Reconcile a tracking reference.
---

import EffectGuide from "../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../shared/core/error.mdx";
import RequestGuide from "../../../../../shared/core/request.mdx";

# getHcaExecutionStatus

Reconcile a tracking reference.

## Import

```ts
import { getHcaExecutionStatus } from "@ensforge/core/hca";
```

## Usage

:::code-group

```ts [index.ts]
import { getHcaExecutionStatus } from "@ensforge/core/hca";
import { config } from "./config";
import { executeHcaCalls } from "@ensforge/core/hca";

const submission = await executeHcaCalls(config, {
  hca: "0x1234567890123456789012345678901234567890",
  authorization: { kind: "owner" },
  calls: [{ to: "0x2345678901234567890123456789012345678901", value: 0n, data: "0x" }],
});

const result = await getHcaExecutionStatus(config, {
  submission,
});
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

## Parameters

```ts
import type { HcaExecutionStatusParameters } from "@ensforge/core/hca";
```

### submission

`HcaExecutionSubmission<unknown>`

Previously returned or explicitly recovered execution submission.

### execution

`ExecutionAdapter<unknown, unknown, unknown> | undefined`

Compatible execution adapter. Omit for direct owner execution when supported.

## Return Type

```ts
import type { HcaExecutionStatus } from "@ensforge/core/hca";
```

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = getHcaExecutionStatus.effect(config, {
  submission,
});

type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;

const result = await Effect.runPromise(program);
```

## Request

<RequestGuide />

```ts
const request = getHcaExecutionStatus.request({
  submission,
});
```

## Error

<ErrorGuide />

## Related

- [`getHcaExecutionStatus`](/sdk/api/hca/get-hca-execution-status)
- [Guide](/core/guides/hca)
