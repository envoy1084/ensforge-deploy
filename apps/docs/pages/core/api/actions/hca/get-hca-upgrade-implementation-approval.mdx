---
title: getHcaUpgradeImplementationApproval
description: Read directional upgrade approval.
---

import EffectGuide from "../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../shared/core/error.mdx";

# getHcaUpgradeImplementationApproval

Read directional upgrade approval.

## Import

```ts
import { getHcaUpgradeImplementationApproval } from "@ensforge/core/hca";
```

## Usage

:::code-group

```ts [index.ts]
import { getHcaUpgradeImplementationApproval } from "@ensforge/core/hca";
import { config } from "./config";

type Input = Parameters<typeof getHcaUpgradeImplementationApproval>[1];
const parameters: Input = {
  implementation: "0x0000000000000000000000000000000000000001",
};

const result = await getHcaUpgradeImplementationApproval(config, parameters);
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

## Parameters

```ts
type Input = Parameters<typeof getHcaUpgradeImplementationApproval>[1];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

### gate

`` `0x${string}` | undefined ``

Optional upgrade-gate address. Defaults to the deployment profile gate.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof getHcaUpgradeImplementationApproval>>;
```

Returns `boolean`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = getHcaUpgradeImplementationApproval.effect(config, parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = getHcaUpgradeImplementationApproval.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`getHcaUpgradeImplementationApproval`](/sdk/api/hca/get-hca-upgrade-implementation-approval)
- [Guide](/core/guides/hca)
