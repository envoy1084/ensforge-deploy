---
title: getHcaCapabilities
description: Inspect supported account and execution capabilities.
---

import EffectGuide from "../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../shared/core/error.mdx";

# getHcaCapabilities

Inspect supported account and execution capabilities.

## Import

```ts
import { getHcaCapabilities } from "@ensforge/core/hca";
```

## Usage

:::code-group

```ts [index.ts]
import { getHcaCapabilities } from "@ensforge/core/hca";
import { config } from "./config";

type Input = Parameters<typeof getHcaCapabilities>[1];
const parameters: Input = {
  hca: "0x0000000000000000000000000000000000000001",
};

const result = await getHcaCapabilities(config, parameters);
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

## Parameters

```ts
type Input = Parameters<typeof getHcaCapabilities>[1];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### expectedOwner

`` `0x${string}` | undefined ``

Expected account owner. A mismatch fails verification.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

### initialImplementation

`` `0x${string}` | undefined ``

Implementation used when deriving the account address, including an account that was later upgraded.

### allowUndeployed

`boolean | undefined`

Allow a counterfactual account after verifying its derivation.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof getHcaCapabilities>>;
```

Returns `HcaCapabilities`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = getHcaCapabilities.effect(config, parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = getHcaCapabilities.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`getHcaCapabilities`](/sdk/api/hca/get-hca-capabilities)
- [Guide](/core/guides/hca)
