---
title: getHcaImplementationApproval
description: Check factory implementation approval.
---

import EffectGuide from "../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../shared/core/error.mdx";

# getHcaImplementationApproval

Check factory implementation approval.

## Import

```ts
import { getHcaImplementationApproval } from "@ensforge/core/hca";
```

## Usage

:::code-group

```ts [index.ts]
import { getHcaImplementationApproval } from "@ensforge/core/hca";
import { config } from "./config";

type Input = Parameters<typeof getHcaImplementationApproval>[1];
const parameters: Input = {
  implementation: "0x0000000000000000000000000000000000000001",
};

const result = await getHcaImplementationApproval(config, parameters);
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

## Parameters

```ts
type Input = Parameters<typeof getHcaImplementationApproval>[1];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### implementation

`` `0x${string}` ``

Implementation address to inspect, approve or use for derivation.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof getHcaImplementationApproval>>;
```

Returns `boolean`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = getHcaImplementationApproval.effect(config, parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = getHcaImplementationApproval.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`getHcaImplementationApproval`](/sdk/api/hca/get-hca-implementation-approval)
- [Guide](/core/guides/hca)
