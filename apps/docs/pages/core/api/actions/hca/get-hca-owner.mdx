---
title: getHcaOwner
description: Read immutable account ownership.
---

import { ReadActionDemo } from "../../../../../components/read-actions/read-action-demo.client.tsx";
import EffectGuide from "../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../shared/core/error.mdx";

# getHcaOwner

Read immutable account ownership.

## Import

```ts
import { getHcaOwner } from "@ensforge/core/hca";
```

## Usage

:::code-group

```ts [index.ts]
import { getHcaOwner } from "@ensforge/core/hca";
import { config } from "./config";

type Input = Parameters<typeof getHcaOwner>[1];
const parameters: Input = {
  hca: "0x0000000000000000000000000000000000000001",
};

const result = await getHcaOwner(config, parameters);
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

<ReadActionDemo action="hca.getHcaOwner" />

## Parameters

```ts
type Input = Parameters<typeof getHcaOwner>[1];
```

### blockNumber

`bigint | undefined`

Block number to read. Cannot be combined with blockTag.

### blockTag

`BlockTag | undefined`

Named block state to read. Cannot be combined with blockNumber.

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

## Return Type

```ts
type Result = Awaited<ReturnType<typeof getHcaOwner>>;
```

Returns `` `0x${string}` | null ``.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = getHcaOwner.effect(config, parameters);
type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;
const result = await Effect.runPromise(program);
```

## Request

```ts
const request = getHcaOwner.request(parameters);
```

Requests compose with ENS read batching.

## Error

<ErrorGuide />

Deployment, account and authorization mismatches remain explicit errors. Reconcile uncertain submissions before retrying a write.

## Related

- [`getHcaOwner`](/sdk/api/hca/get-hca-owner)
- [Guide](/core/guides/hca)
