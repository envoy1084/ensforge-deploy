---
title: revokeHcaSessions
description: Revoke existing sessions using the account nonce.
---

import EffectGuide from "../../../../../shared/core/effect.mdx";
import ErrorGuide from "../../../../../shared/core/error.mdx";

# revokeHcaSessions

Revoke existing sessions using the account nonce.

## Import

```ts
import { revokeHcaSessions } from "@ensforge/core/hca";
```

## Usage

:::code-group

```ts [index.ts]
import { revokeHcaSessions } from "@ensforge/core/hca";
import { config } from "./config";

const result = await revokeHcaSessions(config, {
  hca: "0x1234567890123456789012345678901234567890",
});
```

```ts [config.ts]
// [!include ~/snippets/hca/config.ts]
```

:::

## Parameters

```ts
import type { RevokeHcaSessionsParameters } from "@ensforge/core/hca";
```

### hca

`` `0x${string}` ``

HCA address to inspect or operate on.

### salt

`bigint | undefined`

Account derivation salt. Defaults to the deployment profile canonical salt.

### confirmation

`ConfirmationPolicy | undefined`

Receipt confirmation policy.

### walletClient

`WalletClient | undefined`

Viem wallet override. Defaults to the configured wallet resolver.

### account

`` `0x${string}` | Account | undefined ``

Wallet account override for the operation.

## Return Type

```ts
type RevokeHcaSessionsResult = Awaited<ReturnType<typeof revokeHcaSessions>>;
```

| Property       | Type                         | Description                                              |
| -------------- | ---------------------------- | -------------------------------------------------------- |
| `hash`         | `` `0x${string}` ``          | Submitted transaction hash.                              |
| `receipt`      | `TransactionReceipt \| null` | Confirmed transaction receipt, when requested.           |
| `sessionNonce` | `bigint \| null`             | Nonce used to invalidate previously authorized sessions. |

Returns `RevokeHcaSessionsResult`.

## Effect

<EffectGuide />

```ts
import { Effect } from "effect";

const program = revokeHcaSessions.effect(config, {
  hca: "0x1234567890123456789012345678901234567890",
});

type Success = Effect.Success<typeof program>;
type Failure = Effect.Error<typeof program>;

const result = await Effect.runPromise(program);
```

## Error

<ErrorGuide />

## Related

- [`revokeHcaSessions`](/sdk/api/hca/revoke-hca-sessions)
- [Guide](/core/guides/hca)
